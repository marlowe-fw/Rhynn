#include "Direction.h"

using namespace fwworld;

const Direction Direction::UP = Direction(0);
const Direction Direction::RIGHT = Direction(1);
const Direction Direction::DOWN = Direction(2);
const Direction Direction::LEFT = Direction(4);
